interface:
  display_name: "Pydantic AI Temporal Hardgate"
  short_description: "Temporal + pydantic-ai durable harness 审计"
  default_prompt: "Use $pydantic-ai-temporal-hardgate to audit this repository for Temporal + Pydantic AI durable-execution failures."
policy:
  allow_implicit_invocation: true
