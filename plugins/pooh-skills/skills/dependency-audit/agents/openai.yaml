interface:
  display_name: "Dependency Audit"
  short_description: "Python / TS 依赖边界、循环与 dead-code 审计"
  default_prompt: "Use $dependency-audit to audit this repository for dependency boundaries, cycles, and dead-code signals."
policy:
  allow_implicit_invocation: true
