interface:
  display_name: "Silent Failure Hardgate"
  short_description: "过度防御、静默失败与 fail-loud 审计"
  default_prompt: "Use $overdefensive-silent-failure-hardgate to audit this repository for silent fallbacks, swallowed failures, checker suppressions, and off-camera async loss."
policy:
  allow_implicit_invocation: true
