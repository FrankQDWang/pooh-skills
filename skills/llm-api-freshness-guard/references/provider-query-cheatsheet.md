# Query Cheatsheet

Use these as seeds, not as canned truth.

## Provider-resolved

- `OpenAI Python SDK current Responses API migration from chat.completions`
- `Anthropic TypeScript SDK current Messages API streaming tools`
- `Google GenAI Python SDK current generate content tools streaming`

## Family-resolved

- `Current OpenAI compatible responses or chat completions surface for tool calling structured output streaming`
- `Current Anthropic Messages API surface for messages create streaming tools`
- `Current Google GenAI SDK surface for Python TypeScript generate content tools streaming`

## Wrapper-resolved

- `LiteLLM current provider pass through and compatibility behavior`
- `LangChain current chat adapter provider specific pass through behavior`
- `Vercel AI SDK current provider adapter generateText streamText tools`
